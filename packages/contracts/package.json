{
  "name": "@fulfillpay/contracts",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "echo \"contracts build pending\"",
    "test": "echo \"contracts tests pending\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
