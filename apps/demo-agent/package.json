{
  "name": "@fulfillpay/demo-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "echo \"demo-agent tests pending\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
