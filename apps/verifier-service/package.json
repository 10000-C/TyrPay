{
  "name": "@fulfillpay/verifier-service",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch src/index.ts",
    "test": "tsc -p tsconfig.test.json && node --test .test-dist/tests/verifier-service.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fulfillpay/sdk-core": "workspace:*",
    "@fulfillpay/storage-adapter": "workspace:*",
    "@fulfillpay/zktls-adapter": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "ethers": "^6.16.0"
  }
}
